name: test

on:
  workflow_dispatch:

jobs:
  job-1:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "hello world"
  job-2:
    runs-on: ubuntu-24.04
    steps:
      - run: print("next step")
        shell: python
  job-3:
    runs-on: ubuntu-24.04
    steps:
      - run: print("next step")
        shell: python
    needs:
      - job-1
      - job-2
